{
  "name": "mein-winston-logger",
  "version": "1.0.7",
  "description": "util winston logger package",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "patch": "git add . && git commit -m 'patch' && npm version patch && npm publish",
    "prepublishOnly": "npm run build",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "codechimpz",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.11.18",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "winston": "^3.8.2"
  }
}
